# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
out/
.turbo/
.vercel/
.netlify/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# IDE and editors
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Testing
coverage/
.nyc_output/
test-results/
playwright-report/
playwright/.cache/

# TypeScript
*.tsbuildinfo
tsconfig.tsbuildinfo

# Database files
*.db
*.sqlite
*.sqlite3
<% if (database === 'postgresql') { -%>
# PostgreSQL
*.pgsql
<% } -%>
<% if (database === 'mysql') { -%>
# MySQL
*.mysql
<% } -%>

# Prisma
<% if (orm === 'prisma') { -%>
prisma/generated/
prisma/*.db
prisma/*.db-journal
<% } -%>

# Drizzle
<% if (orm === 'drizzle') { -%>
drizzle/meta/
*.sql.ts.map
<% } -%>

# Cache
.cache/
.parcel-cache/
.eslintcache
.stylelintcache
*.cache

# Debug
.debug/
debug.log

# Secrets and credentials (NEVER commit these)
*.pem
*.key
*.crt
*.p12
*.pfx
credentials.json
secrets.json
service-account*.json

# Temporary files
tmp/
temp/
*.tmp
*.temp

# Package manager lock files (keep one, ignore others)
<% if (packageManager === 'pnpm') { -%>
package-lock.json
yarn.lock
bun.lockb
<% } else if (packageManager === 'yarn') { -%>
package-lock.json
pnpm-lock.yaml
bun.lockb
<% } else if (packageManager === 'bun') { -%>
package-lock.json
pnpm-lock.yaml
yarn.lock
<% } else { -%>
pnpm-lock.yaml
yarn.lock
bun.lockb
<% } -%>

# Storybook
storybook-static/

# Documentation generated
docs/api/
api-docs/

# Misc
*.pid
*.seed
*.pid.lock

# Docker
.docker/

# AWS
.aws/

# Serverless
.serverless/

# Optional: Local development database data
data/
postgres-data/
mysql-data/
redis-data/
